# =============================================================================
# Copy to .env next to docker-compose.yml (do not commit secrets).
# =============================================================================

# Must match worker.worker_id in config/worker.yml (overrides yaml when worker-api starts).
WORKER_ID=worker-a
